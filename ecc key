from cryptography.hazmat.primitives.asymmetric import ec
from cryptography.hazmat.primitives import serialization

# Generate ECC private key
my_ecc_key = ec.generate_private_key(ec.SECP256R1())

# Extract public key
public_key = my_ecc_key.public_key()

# Save private key to disk
with open("my_private_key.pem", "wb") as f:
    f.write(my_ecc_key.private_bytes(
        encoding=serialization.Encoding.PEM,
        format=serialization.PrivateFormat.PKCS8,
        encryption_algorithm=serialization.NoEncryption()
    ))

# Save public key to disk
with open("my_public_key.pem", "wb") as f:
    f.write(public_key.public_bytes(
        encoding=serialization.Encoding.PEM,
        format=serialization.PublicFormat.SubjectPublicKeyInfo
    ))

print(" ECC key pair saved to disk.")
